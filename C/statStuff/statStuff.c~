#include <sys/types.h>
#include <sys/stat.h>
#include <unistd.h>

void main(){
  
  struct stat *p = (struct stat*)malloc(sizeof(struct stat));
  int test = stat("statStuff.c",p);

  printf("statStuff.c mode: %d",(*p).st_atime);

}
