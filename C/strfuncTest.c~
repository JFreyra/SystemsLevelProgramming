#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void printer(char *arr);
int myStrlen(char *arr);
char *myStrncpy(char *arr1, char *arr2, int n);

void main(){

  char s[] = "Tuesday";
  char d[] = "Friday";

  //strlen
  printf("length of s: %d",myStrlen(s));
  printf("length of s: %d",myStrlen(s));
  //strncpy
  printer(strcpy(s,d));
  //strcat
  
  //strcmp

  //strchr
}

int myStrlen(char *arr){
  int i = 0;
  while(*arr){
    i++;
    arr++;
  }
  return i;
}

char *myStrncpy(char *arr1, char *arr2, int n){
  
}

void printer(char *arr){
  while(*arr){
    printf("%s",arr);
  }
  printf("\n");
}
